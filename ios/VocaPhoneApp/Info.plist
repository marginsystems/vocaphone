<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- ProMotion is opt-in per bundle, and this is a keyboard: every animation
       it has is a direct response to a finger. Without this key iOS clamps
       Core Animation and every CADisplayLink in the process to 60Hz on an
       iPhone, which silently ignored the trail's own request for the panel's
       full rate. -->
  <key>CADisableMinimumFrameDurationOnPhone</key>
  <true/>
  <!-- The App Group this bundle's entitlement grants, carried into the bundle
       so the code can ask rather than hardcode it. A device build signed under
       a personal team renames the group; a constant in Swift does not follow,
       and the app then opens a container nobody wrote to. -->
  <key>VocaPhoneAppGroup</key>
  <string>$(VOCAPHONE_APP_GROUP)</string>
  <key>CFBundleDisplayName</key>
  <string>vocaphone</string>
  <key>CFBundleExecutable</key>
  <string>$(EXECUTABLE_NAME)</string>
  <key>CFBundleIdentifier</key>
  <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
  <key>CFBundleName</key>
  <string>$(PRODUCT_NAME)</string>
  <key>CFBundlePackageType</key>
  <string>APPL</string>
  <key>CFBundleShortVersionString</key>
  <string>$(MARKETING_VERSION)</string>
  <key>CFBundleVersion</key>
  <string>$(CURRENT_PROJECT_VERSION)</string>
  <key>LSRequiresIPhoneOS</key>
  <true/>
  <!-- VocaPhone checks the installed-app scheme for X. iOS returns false
       from canOpenURL for undeclared schemes, which otherwise made the Stats
       X button choose the browser even when the app was present. -->
  <key>LSApplicationQueriesSchemes</key>
  <array>
    <string>twitter</string>
  </array>
  <key>NSMicrophoneUsageDescription</key>
  <string>vocaphone records dictation for your configured transcription gateway and, when Quick Dictation is enabled, keeps microphone input ready for up to 10 minutes. Standby audio is discarded.</string>
  <key>NSCameraUsageDescription</key>
  <string>vocaphone uses the camera only when you choose to scan the gateway pairing QR code.</string>
  <key>NSLocalNetworkUsageDescription</key>
  <string>vocaphone connects to the transcription gateway you configure on your private network.</string>
  <key>NSAppTransportSecurity</key>
  <dict>
    <!-- ATS has no CIDR-based exception, and neither NSAllowsLocalNetworking
         nor an NSExceptionDomains entry for ts.net covers a bare Tailscale
         CGNAT IP (100.64.0.0/10) or other private hosts typed by hand.
         GatewayEndpoint.isPrivateNetworkHost is the real gate: it already
         restricts plain-HTTP gateways to LAN/CGNAT/mDNS/tailnet hosts before
         a request is ever made, matching the Android cleartext policy. -->
    <key>NSAllowsArbitraryLoads</key>
    <true/>
  </dict>
  <key>NSSupportsLiveActivities</key>
  <true/>
  <key>ITSAppUsesNonExemptEncryption</key>
  <false/>
  <key>UIBackgroundModes</key>
  <array>
    <string>audio</string>
  </array>
  <key>CFBundleURLTypes</key>
  <array>
    <dict>
     <key>CFBundleURLName</key>
       <string>com.vocahq.vocaphone.dictation</string>
       <key>CFBundleURLSchemes</key>
       <array>
         <string>vocaphone</string>
      </array>
    </dict>
  </array>
  <key>UILaunchScreen</key>
  <dict/>
  <key>UISupportedInterfaceOrientations</key>
  <array>
    <string>UIInterfaceOrientationPortrait</string>
  </array>
</dict>
</plist>
