<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
      Normal permission, granted at install with no prompt. Needed only to route
      capture to a Bluetooth headset's microphone when the user selects one:
      Android reaches that microphone through the communication device, not
      through AudioRecord's preferred device.
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Normal permission. Dictation start/stop haptic uses Vibrator. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Used only when the user taps Scan QR on the gateway screen. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Stats sharing checks only X's package before choosing the web fallback. -->
    <queries>
        <package android:name="com.twitter.android" />
    </queries>

    <application
        android:name=".VocaPhoneApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.VocaPhone">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The documented fallback for Android versions that refuse a background
          foreground-service start from a keyboard or companion-app action. It is
          invisible and closes immediately after starting the service.
        -->
        <activity
            android:name=".dictation.DictationLauncherActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.VocaPhone.Invisible" />

        <activity
            android:name=".ui.QrPairingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.VocaPhone" />

        <service
            android:name=".dictation.DictationService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- The keyboard writes through InputConnection and never reads field text. -->
        <service
            android:name=".ime.VocaPhoneInputMethodService"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.clipboard"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/clipboard_paths" />
        </provider>

    </application>
</manifest>
